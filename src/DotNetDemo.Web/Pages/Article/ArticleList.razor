@page "/articles"
@inject Services.IArticleService Service
<div>
    <h3>Articles</h3>
    <div class="form-group">
        <a class="btn btn-success" href="createemployee"><i class="oi oi-plus"></i> Create New</a>
    </div>
</div>
<div>
    @if (articles != null) {
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">
                        ID
                    </th>
                    <th scope="col">
                        Name
                    </th>
                    <th scope="col">
                        Gender
                    </th>
                    <th scope="col">
                        DOB
                    </th>
                    <th scope="col">
                        Department
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var employee in articles) {
                    <tr>
                        <td>
                            @employee.ArticleId
                        </td>
                        <td>
                            <a href=@($"employeedetails/{employee.ArticleId}")>@employee.ArticleId</a>
                        </td>
                        <td>
                            @employee.Subject
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>
@code {
    private IEnumerable<Models.Article> articles;

    protected override async Task OnInitializedAsync()
    {
        articles = await Service.SearchArticles();
    }
}
